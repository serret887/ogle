package ogle

import (
	"errors"

	"github.com/serret887/ogle/matcher"
	"golang.org/x/net/html"
)

// Find allwo to find nodes that match all the Matchers
func (o *Ogle) Find(match ...matcher.Match) ([]html.Node, error) {

	return nil, errors.New("There are no tag a")
}
